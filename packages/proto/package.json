{
  "name": "@crypto-tracker/proto",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "generate": "buf generate",
    "lint": "buf lint",
    "breaking": "buf breaking --against '.git#branch=main'"
  }
}